# RAG 检索增强生成

## 课程简介

RAG（Retrieval-Augmented Generation，检索增强生成）是当前 AI Agent 最核心的技术之一。它通过从外部知识库中检索相关信息，增强 LLM 的回答质量和时效性。本课时将深入学习 RAG 的原理、架构和实现方法。

## 为什么需要 RAG

### LLM 的固有局限

| 问题 | 说明 | RAG 解决方案 |
|------|------|-------------|
| 知识截止 | 训练数据有截止日期 | 检索最新的文档 |
| 幻觉问题 | LLM 可能生成不准确的信息 | 基于检索到的原文回答 |
| 领域知识 | 通用 LLM 缺乏专业知识 | 注入领域文档 |
| 隐私数据 | 不能将敏感数据用于训练 | 仅检索不暴露原始数据 |

## RAG 架构

```
┌──────────────────────────────────────────────────────────┐
│                      RAG 系统架构                          │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐          │
│  │ 文档加载  │───►│ 文档切分  │───►│ Embedding│          │
│  │ (Loader) │    │(Splitter) │    │ (向量化) │          │
│  └──────────┘    └──────────┘    └────┬─────┘          │
│                                      │                   │
│                                      ▼                   │
│                              ┌──────────────┐           │
│                              │ 向量数据库    │           │
│                              │ (Vector DB)  │           │
│                              └──────┬───────┘           │
│                                     │                    │
│  ┌──────────┐    ┌──────────┐       │    ┌──────────┐  │
│  │ 用户查询  │───►│ 查询向量化│◄──────┘    │  LLM 生成  │  │
│  │ (Query)  │    │ + 检索   │             │ + 上下文   │  │
│  └──────────┘    └──────────┘             └──────────┘  │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

## 代码示例：完整 RAG 实现

```python
from typing import List, Dict
import numpy as np

class SimpleRAG:
    """简化版 RAG 系统"""

    def __init__(self, embedding_dim=128):
        self.documents = []      # 文档库
        self.embeddings = []     # 文档向量
        self.embedding_dim = embedding_dim

    def add_documents(self, docs: List[str]):
        """添加文档到知识库"""
        for doc in docs:
            self.documents.append(doc)
            embedding = self._simple_embed(doc)
            self.embeddings.append(embedding)

    def _simple_embed(self, text: str) -> List[float]:
        """简单的词袋模型嵌入（生产环境应使用 sentence-transformers）"""
        # 基于字符频率的简单嵌入
        vocab = {}
        for char in set(text):
            vocab[char] = text.count(char) / len(text)

        embedding = [0.0] * self.embedding_dim
        for i, char in enumerate(sorted(vocab.keys())):
            if i < self.embedding_dim:
                embedding[i] = vocab[char]
        return embedding

    def _cosine_similarity(self, a, b):
        """计算余弦相似度"""
        a, b = np.array(a), np.array(b)
        return np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b))

    def retrieve(self, query: str, top_k: int = 3) -> List[Dict]:
        """检索最相关的文档片段"""
        query_embedding = self._simple_embed(query)

        scores = []
        for i, doc_embedding in enumerate(self.embeddings):
            score = self._cosine_similarity(query_embedding, doc_embedding)
            scores.append({
                "index": i,
                "score": score,
                "content": self.documents[i],
            })

        scores.sort(key=lambda x: x["score"], reverse=True)
        return scores[:top_k]

    def generate_response(self, query: str, llm) -> str:
        """RAG 生成回答"""
        # 1. 检索相关文档
        relevant_docs = self.retrieve(query, top_k=3)

        # 2. 构建增强 Prompt
        context = "\n\n".join([
            f"[参考 {i+1}]: {doc['content']}"
            for i, doc in enumerate(relevant_docs)
        ])

        prompt = f"""基于以下参考信息回答问题。如果参考信息中没有答案，请明确说明。

参考信息：
{context}

问题：{query}

回答："""

        # 3. 让 LLM 生成回答
        response = llm.generate(prompt)
        return response


# 使用示例
knowledge_base = [
    "Python 是一种高级编程语言，由 Guido van Rossum 于 1991 年创建。",
    "Python 的设计哲学强调代码的可读性和简洁性。",
    "Python 支持多种编程范式：面向对象、函数式、过程式。",
    "Python 的标准库非常丰富，包括网络、数据库、GUI 等模块。",
    "Python 是数据科学和人工智能领域最流行的语言之一。",
    "Django 和 Flask 是 Python 最流行的 Web 框架。",
    "Python 的 GIL（全局解释器锁）限制了多线程 CPU 密集型任务的性能。",
]

rag = SimpleRAG()
rag.add_documents(knowledge_base)

# 查询
query = "Python 是谁创建的？有什么特点？"
results = rag.retrieve(query)
print("检索到的相关内容:")
for r in results:
    print(f"  [{r['score']:.3f}] {r['content']}")
```

## 文档切分策略

```python
class DocumentSplitter:
    """文档切分器"""

    def split_by_chars(self, text, chunk_size=500, overlap=50):
        """按字符数切分"""
        chunks = []
        start = 0
        while start < len(text):
            end = start + chunk_size
            chunk = text[start:end]
            chunks.append(chunk)
            start = end - overlap  # 重叠区域保证上下文连续性
        return chunks

    def split_by_tokens(self, text, chunk_size=100, overlap=10):
        """按 Token 数切分（更精确）"""
        tokens = text.split()
        chunks = []
        start = 0
        while start < len(tokens):
            end = start + chunk_size
            chunk = " ".join(tokens[start:end])
            chunks.append(chunk)
            start = end - overlap
        return chunks

    def split_by_semantic(self, text):
        """按语义边界切分（按段落/标题）"""
        # 按 Markdown 标题切分
        sections = []
        current_section = ""
        for line in text.split("\n"):
            if line.startswith("#"):
                if current_section:
                    sections.append(current_section.strip())
                current_section = line + "\n"
            else:
                current_section += line + "\n"
        if current_section:
            sections.append(current_section.strip())
        return sections
```

## 向量数据库选型

| 数据库 | 特点 | 适用场景 |
|--------|------|----------|
| **Chroma** | 轻量、易用、Python 原生 | 原型开发、小规模应用 |
| **Pinecone** | 托管服务、高可用 | 生产环境、大规模 |
| **Qdrant** | 开源、高性能、Rust 编写 | 自托管、高性能需求 |
| **Weaviate** | 模块化、支持混合搜索 | 企业级应用 |
| **FAISS** | Meta 开源、极致性能 | 离线大规模检索 |

## 高级 RAG 技术

### 1. 混合搜索（Hybrid Search）

结合向量搜索和关键词搜索：

```python
def hybrid_search(query, vector_db, keyword_index, alpha=0.5):
    """混合搜索：向量相似度 + 关键词匹配"""
    # 向量搜索
    vector_results = vector_db.search(query, top_k=10)

    # 关键词搜索
    keyword_results = keyword_index.search(query, top_k=10)

    # 融合排序（ Reciprocal Rank Fusion）
    scores = {}
    for rank, result in enumerate(vector_results):
        scores[result["id"]] = scores.get(result["id"], 0) + alpha * (1 / (rank + 1))
    for rank, result in enumerate(keyword_results):
        scores[result["id"]] = scores.get(result["id"], 0) + (1 - alpha) * (1 / (rank + 1))

    return sorted(scores.items(), key=lambda x: x[1], reverse=True)
```

### 2. 重排序（Reranking）

检索后再用交叉编码器精排：

```
初步检索（向量搜索，召回 20 条）
        │
        ▼
重排序模型（Cross-Encoder，精排 Top 5）
        │
        ▼
返回最相关的 5 条给 LLM
```

## 练习任务

1. 实现一个基于字符频率的简单向量嵌入函数
2. 用余弦相似度实现文档检索，测试不同查询的效果
3. 对比"按字符切分"和"按段落切分"两种策略的检索效果

## 知识点总结

- RAG = 检索（Retrieval） + 增强（Augmented） + 生成（Generation）
- 核心流程：文档加载 → 切分 → Embedding → 向量存储 → 检索 → LLM 生成
- 文档切分是 RAG 质量的关键因素
- 向量数据库存储和检索高维向量
- 混合搜索（向量 + 关键词）通常比单一搜索效果更好

> 💡 提示：RAG 的精髓不是"找到正确答案"，而是"提供足够的上下文让 LLM 自己找到正确答案"。
